import type {IfDefined, Resolver} from '@typeschema/core';

export interface AdapterResolver extends Resolver {
  base: IfDefined<$FixMe, '$FixMe'>;
  input: this['schema'] extends this['base'] ? $FixMe : never;
  output: this['schema'] extends this['base'] ? $FixMe : never;
}
