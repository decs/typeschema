{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "tsconfig.json",
    "tsconfig.test.json",
    ".eslintrc.json",
    ".prettierrc.json",
    "tsup.config.ts",
    "vitest.config.ts"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "lint:package": {
      "dependsOn": ["build"]
    },
    "test": {
      "cache": false,
      "dependsOn": ["^build"]
    },
    "upgrade:deps": {}
  }
}
